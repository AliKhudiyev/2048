#include"texture.hpp"
#include"surface.hpp"

Texture* Texture::on_load(const char* name){
    Texture* texture=nullptr;
    SDL_Surface* surface=nullptr;

    // if(!(surface=Surface::on_load(name))) return NULL;


    return texture;
}